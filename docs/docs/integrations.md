# Integrations

## Spring
Spring integration is being rebuilt. Until then, prefer plain `mockk()`/`spyk()` in your tests instead of `@MockBean`/`@SpyBean`.

## Quarkus
[quarkus-mockk](https://github.com/quarkiverse/quarkus-mockk) adds support for mocking beans in Quarkus.
